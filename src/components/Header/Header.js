import Navbar from "../Navbar/Navbar";
import { Container } from "./Header.elements";

const Header = () => {
  return (
    <Container>
      <Navbar />
    </Container>
  );
};

export default Header;
