import styled from "styled-components";

export const CartContainer = styled.div`
  font-size: 3rem;

  ul {
    display: flex;
    flex-direction: column;
  }

  img {
    max-width: 20%;
  }

  p {
    font-weight: 700;
  }
`;
